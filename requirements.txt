# Runtime deps for the Streamlit app — pinned for reproducible installs.
# Inference runs on CPU via a TorchScript model (no fastai required).
streamlit==1.41.1
numpy==1.26.4
pillow==10.4.0
torch==2.12.0
torchvision==0.17.0
opencv-python-headless==4.9.0.80
